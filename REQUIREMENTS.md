# **one-tracker - One-API 系列网站模型价格比价平台 (PRD)**

版本: 1.0  
日期: 2025-08-21  
项目代号: "one-tracker"

## 1. 项目愿景与目标

### 1.1 核心愿景

one-tracker 是一个部署在 Cloudflare 全栈架构上的「One-API 系列网站模型价格比价平台」。平台允许用户自行添加价格源站与充值比例，统一换算为真实 Token 成本（标准化计价单位），并提供按模型维度的跨站比价与变更追踪。

### 1.2 解决的核心问题

- 供应商定价口径不统一（每 1K / 1M Token、输入/输出分档、模型分组、站内充值倍率等），难以横向比较。
- 同一模型在不同站点的「折算真实成本」差异大，且价格经常波动，人工追踪成本高。
- 用户需要基于自身偏好（币种、充值倍率、使用占比）快速选出最具性价比的站点与模型。

### 1.3 目标用户

- 使用 One-API 或其他衍生中转站的开发者与团队。
- 需要精确控制推理成本的产品工程团队、提示工程团队、数据团队。
- 运营/采买站长：需要监控上游价格波动、选择合适的模型供应商。

## 2. 核心功能

### 2.1 账户与鉴权

- 保留 GitHub OAuth 登录（唯一社交登录方式）。
- 登录后拥有个人空间，可维护个人价格源配置、对比视图偏好等。

### 2.2 价格源站管理

- 新增/编辑/删除价格源站：
- 字段：`name`、`pricingEndpoint`（如可公开访问的价格接口）、`rechargeRatio`（充值倍率系数）、`notes`、`status`。
- 价格抓取：
  - 手动触发拉取
  - 失败重试与退避，错误可视化。

### 2.3 模型目录与标准化

- 维护「标准模型目录」（canonical model registry），汇总热门模型（如 gpt-4o、gpt-4o-mini、o3 系列、claude 3.5/3.7、deepseek、gemini 等）。
- 站点返回的模型名通过「站点模型名 → 标准模型名」映射归一。
- 支持多模态与不同计价维度（输入 tokens、输出 tokens、请求起价、图像/音频/推理等附加项）。

### 2.4 价格标准化与计算

- 目标口径：统一折算到「每 1K/1M 输入/输出 Token 的真实成本」，默认显示每 1M Token 成本（可切换每 1K）。
- 处理要素：
  - 站点充值倍率 `rechargeRatio`：1货币单位能兑换该站点的余额值（具体货币单位不重要，用户填写即可）。
  - 分段/阶梯价与包月价（首期版本仅记录，标准化以按量价为主）。
- 标准化计算（示例）：
  - 设站点给出「每 1K 输入 token 价格 p_in」「每 1K 输出 token 价格 p_out」。
  - 若 `rechargeRatio` 为将货币换算为站内余额的倍率，则统一口径：
    - 若返回价为货币直给：`normalizedPrice = p / rechargeRatio`
    - 若返回价为站内余额单位：先将余额单价换算为货币后再归一。
  - 最终产出：`pricePer1MInput`, `pricePer1MOutput`，以及 `pricePer1K*` 的派生值。
- 界面显示：
  - 计算逻辑不受币种影响，最终结果是1M token在该货币单位下的价格
  - 界面默认显示¥符号，用户可切换其他货币单位显示
  - 货币切换仅影响显示，不影响计算逻辑和存储

### 2.5 比价与筛选

#### 2.5.1 核心比价功能

- **模型比价表**：按标准模型列出各站点的折算成本，支持排序/过滤。
- **实时价格计算**：基于用户配置的供应商和分组，实时计算综合价格。
- **供应商选择**：用户可为每个模型选择特定供应商，默认选择最低价，支持持久化存储。
- **分组切换**：支持为特定供应商的特定模型切换使用分组，影响价格计算。

#### 2.5.2 高级筛选与控制

- **搜索功能**：支持模型名称、厂商、功能标签的模糊搜索。
- **多维度过滤**：
  - 厂商筛选：按模型厂商（OpenAI、Anthropic、Google等）过滤
  - 供应商数量筛选：筛选单一供应商、多个供应商或指定数量供应商的模型
  - 价格区间筛选：按价格范围筛选模型
  - 收藏/隐藏：支持收藏感兴趣的模型，隐藏不关注的模型
- **排序选项**：支持按字母序、价格、供应商数量等维度排序
- **自定义使用画像**：
  - 输入:输出占比设置（如 7:3），按画像计算加权总成本排序
  - 实时防抖调节，支持精细化权重设置
  - 1K/1M Token 单位切换显示

#### 2.5.3 批量操作

- **一键选择最低价**：为所有筛选模型批量选择最低价供应商
- **批量分组管理**：支持批量启用/禁用供应商分组

### 2.6 供应商概览与分析

#### 2.6.1 供应商统计面板

- **全局统计仪表板**：
  - 总模型数量：当前系统支持的所有模型数量
  - 竞争模型数量：有多个供应商提供的模型数量
  - 独家模型数量：只有单一供应商提供的模型数量
  - 竞争率：有竞争模型占总模型的百分比
- **供应商详细卡片**：
  - 基础信息：供应商名称、类型、支持模型数量
  - 竞争力指标：最低价模型数量（仅统计有竞争的模型）、平均价格排名
  - 覆盖范围：支持的模型厂商列表、独家模型数量
  - 分组管理：分组启用状态、平均倍率显示

#### 2.6.2 集成分组管理

- **可视化分组状态**：进度条显示分组启用比例
- **分组详情展示**：
  - 分组名称、倍率（1x、0.5x、2x格式显示）
  - 影响模型数量统计
  - 颜色编码：优惠（绿色）、原价（默认）、加价（红色）
- **批量分组操作**：全部启用/全部禁用快捷操作

#### 2.6.3 匿名显示模式

- **隐私保护**：支持匿名显示供应商和分组名称
- **一致性映射**：基于哈希算法确保同一供应商/分组始终映射到相同匿名名称
- **主题化命名**：
  - 供应商：魔法主题名称（星辰法师、火焰巫师等）
  - 分组：樱花/晴空主题名称（樱花飞舞、晴空万里等）

### 2.7 性能优化与用户体验

#### 2.7.1 性能优化策略

- **分页显示**：模型列表分页展示，每页最多100个模型，避免DOM节点过多
- **防抖机制**：权重滑块、搜索输入等交互使用防抖，减少计算压力
- **数据预计算**：供应商统计、排序等数据使用useMemo预计算
- **加载动画**：数据处理过程提供骨架屏和进度指示

#### 2.7.2 交互体验优化

- **即时反馈**：所有用户操作提供即时视觉反馈
- **状态持久化**：用户选择、筛选条件等状态本地存储
- **智能默认**：自动选择最低价供应商、最低倍率分组等
- **视觉层次**：清晰的信息层次和颜色编码系统

### 2.8 价格变更追踪与告警

- 记录每日（或更细粒度）标准化价格快照。
- 展示价格曲线与变动百分比。
- 支持阈值告警（降价/涨价、波动率）与订阅（站点/模型）。

## 3. 非功能性要求

- 性能：比价主查询 P95 < 200ms（缓存命中场景）。
- 可用性：抓取失败不影响既有价格展示（兜底展示最近成功快照）。
- 安全：站点私有认证信息加密存储与访问最小化；后端不对外透出敏感字段。
- 可观测性：抓取/标准化/渲染全链路日志与统计指标。

## 4. 技术架构

### 4.1 基础架构

- 运行环境：Cloudflare Workers + Pages（混合渲染 Hono + React）。
- 数据库：Cloudflare D1（业务数据）+ KV（缓存/配置）。
- 后端：Hono + TypeScript，采用 OpenAPI 与 zod 模式定义。
- 前端：React + Vite，统一路由定义与 SSR/Hydrate。
- 任务编排：Cron Triggers 定时拉取与重试；Queues（可选）做异步标准化流水线。

### 4.2 前端组件架构

#### 4.2.1 页面级组件

- **PricingComparePage** (`/frontend/src/pages/PricingComparePage.tsx`)：
  - 比价页面主入口，协调所有子组件
  - 管理全局状态：供应商配置、模型数据、筛选条件、用户选择
  - 实现分页逻辑、加载动画、匿名模式控制
  - 集成所有业务逻辑：数据获取、价格计算、状态持久化

#### 4.2.2 核心业务组件

- **ProviderConfigPanel** (`/frontend/src/components/pricing/ProviderConfigPanel.tsx`)：
  - 供应商配置管理：添加、编辑、删除供应商
  - 高级配置：价格基准、颜色自定义、模型过滤
  - 数据获取控制：拉取价格、重置数据、锁定配置
  - 表单验证和错误处理

- **ModelComparisonCard** (`/frontend/src/components/pricing/ModelComparisonCard.tsx`)：
  - 单个模型的价格比较展示
  - 左右布局：模型信息（左）+ 供应商价格卡片（右）
  - 供应商选择：支持用户选择特定供应商，显示选中状态
  - 分组切换：支持切换供应商分组，实时更新价格
  - 价格计算：显示综合价格和详细计算公式（鼠标悬浮）

- **ProviderOverview** (`/frontend/src/components/pricing/ProviderOverview.tsx`)：
  - 供应商概览和统计分析
  - 全局统计仪表板：模型数量、竞争情况分析
  - 供应商详细卡片：竞争力指标、覆盖范围、分组管理
  - 集成分组管理：可视化分组状态、批量操作

- **PricingControlPanel** (`/frontend/src/components/pricing/PricingControlPanel.tsx`)：
  - 全局筛选和控制面板
  - 搜索功能：模型名称、厂商模糊搜索
  - 高级筛选：厂商、供应商数量、价格区间
  - 使用场景权重：输入/输出比例调节
  - 显示选项：1K/1M单位切换、排序方式

#### 4.2.3 工具组件

- **ModelSearchBar** (`/frontend/src/components/pricing/ModelSearchBar.tsx`)：
  - 模型搜索输入框
  - 支持实时搜索、防抖处理
  - 搜索历史和建议（扩展）

#### 4.2.4 自定义Hooks

- **useModelFiltering** (`/frontend/src/hooks/useModelFiltering.ts`)：
  - 模型筛选和排序逻辑
  - 支持多维度筛选：厂商、供应商数量、价格区间、搜索关键词
  - 排序算法：字母序、价格、供应商数量

- **useProviderSelection** (`/frontend/src/hooks/useProviderSelection.ts`)：
  - 供应商选择状态管理
  - 本地存储持久化
  - 批量操作支持

- **useGroupSelection** (`/frontend/src/hooks/useGroupSelection.ts`)：
  - 分组选择状态管理
  - 智能默认选择（最低倍率分组）
  - 状态持久化

### 4.3 数据流架构

#### 4.3.1 状态管理策略

- **页面级状态**：使用 React useState 管理复杂业务状态
- **持久化状态**：localStorage 存储用户选择和偏好设置
- **计算状态**：useMemo 优化重计算，useCallback 优化函数引用

#### 4.3.2 价格计算流程

1. **数据标准化**：原始供应商数据 → 标准化价格记录
2. **实时计算**：基于用户选择的供应商和分组计算综合价格
3. **公式透明化**：鼠标悬浮显示详细计算过程
4. **缓存优化**：预计算常用数据，减少重复计算

#### 4.3.3 性能优化策略

- **虚拟化渲染**：分页显示减少DOM节点
- **防抖处理**：滑块、输入框等交互防抖
- **懒加载**：按需加载组件和数据
- **缓存策略**：计算结果缓存、状态预计算

## 5. 当前实现功能清单

### 5.1 已实现核心功能

#### 5.1.1 供应商管理

- ✅ **动态供应商配置**：支持 OneHub、NewAPI、OneAPI 三种类型
- ✅ **实时价格拉取**：手动触发价格数据获取
- ✅ **高级配置选项**：
  - 价格基准调整（默认0.002）
  - 供应商颜色自定义（支持颜色选择器）
  - 模型过滤：黑名单/白名单模式，支持多关键词
- ✅ **数据验证**：URL格式验证、重复检查、错误处理

#### 5.1.2 模型比价核心

- ✅ **标准化价格显示**：统一为1K/1M Token成本
- ✅ **实时价格计算**：基于REQUIREMENTS.md公式的准确计算
- ✅ **供应商选择**：
  - 默认选择最低价供应商
  - 用户可手动切换选择
  - 选择状态本地持久化
- ✅ **分组切换**：
  - 支持切换供应商分组
  - 智能默认选择最低倍率分组
  - 实时更新价格显示
- ✅ **价格公式透明化**：鼠标悬浮显示详细计算过程

#### 5.1.3 高级筛选系统

- ✅ **多维度筛选**：
  - 模型名称/厂商搜索（防抖）
  - 厂商筛选（OpenAI、Anthropic等）
  - 供应商数量筛选（单一、多个、指定数量）
  - 收藏/隐藏模型管理
- ✅ **排序选项**：字母序、价格、供应商数量
- ✅ **使用场景权重**：输入/输出比例调节（防抖优化）
- ✅ **显示选项**：1K/1M Token单位切换

#### 5.1.4 供应商概览分析

- ✅ **全局统计仪表板**：
  - 总模型数、竞争模型数、独家模型数
  - 竞争率分析
- ✅ **供应商详细统计**：
  - 支持模型数量、最低价模型数量（仅统计有竞争的）
  - 平均价格排名、支持厂商列表
  - 独家模型数量、平均分组倍率
- ✅ **集成分组管理**：
  - 可视化分组启用状态（进度条）
  - 分组详情：倍率显示（1x、0.5x格式）、影响模型数
  - 批量操作：全部启用/禁用

#### 5.1.5 性能与体验优化

- ✅ **分页显示**：每页100个模型，避免性能问题
- ✅ **加载动画**：数据处理过程的骨架屏和进度指示
- ✅ **防抖优化**：权重滑块、搜索输入等交互优化
- ✅ **状态持久化**：用户选择和偏好设置本地存储
- ✅ **响应式设计**：适配不同屏幕尺寸

#### 5.1.6 隐私保护功能

- ✅ **匿名显示模式**：
  - 供应商匿名化（魔法主题命名）
  - 分组匿名化（樱花/晴空主题命名）
  - 基于哈希的一致性映射
- ✅ **截图友好**：便于供应商查看时保护敏感信息

### 5.2 技术实现亮点

#### 5.2.1 架构设计

- ✅ **模块化组件**：清晰的组件职责分离
- ✅ **自定义Hooks**：业务逻辑复用和状态管理
- ✅ **类型安全**：完整的TypeScript类型定义
- ✅ **主题系统**：统一的颜色和样式管理

#### 5.2.2 数据处理

- ✅ **标准化解析**：支持多种供应商API格式
- ✅ **错误处理**：优雅的错误处理和用户提示
- ✅ **数据验证**：严格的数据一致性检查
- ✅ **缓存优化**：智能的数据缓存和预计算

### 5.3 待扩展功能

#### 5.3.1 短期扩展

- 🔄 **价格历史追踪**：记录价格变动历史
- 🔄 **告警系统**：价格变动提醒
- 🔄 **批量操作增强**：更多批量选择选项
- 🔄 **导出功能**：价格对比数据导出

#### 5.3.2 中长期规划

- 🔄 **用户账户系统**：个人配置云端同步
- 🔄 **API接口**：第三方集成支持
- 🔄 **高级分析**：价格趋势分析、成本优化建议
- 🔄 **多语言支持**：国际化扩展

## 7. 数据模型（D1，简化首版）

- `users`：`id (pk)`, `github_id`, `name`, `avatar`, `created_at`。
- `provider_endpoints`：`id (pk)`, `user_id (fk)`, `name`, `pricing_endpoint`, `recharge_ratio`, `auth_header`, `auth_query`, `notes`, `status`, `created_at`, `updated_at`。
- `model_canonicals`：`id (pk)`, `canonical_name`, `vendor`, `modality`, `notes`。
- `model_aliases`：`id (pk)`, `provider_id (fk)`, `alias_name`, `canonical_id (fk)`。
- `price_snapshots`：`id (pk)`, `provider_id (fk)`, `captured_at`, `raw_payload (json)`。
- `normalized_prices`：`id (pk)`, `provider_id`, `canonical_id`, `captured_at`, `price_per_1k_input`, `price_per_1k_output`, `price_per_1m_input`, `price_per_1m_output`, `meta (json)`。
- `alerts`：`id (pk)`, `user_id`, `type`, `rule (json)`, `active`, `created_at`。

说明：

- `price_snapshots` 保存源站原始返回，`normalized_prices` 存标准化结果，读多写少便于缓存。
- `model_aliases` 解决不同站点模型名不一致的问题。

## 8. 外部价格接口与解析

### 8.1 统一标准化记录（目标格式）

为同时支持不同来源结构与未来扩展，定义统一的标准化记录格式：

```jsonc
{
  "source": { // 源站信息
    "name": "xxx API",
    "url": "http...",
    "type": "onehub",  // 源站类型，目前先支持 NewAPI / OneHub，访问endpoint等不需要在结构中给出，而是由具体策略实现
    "rechargeRatio": 1,  // 充值比例，即 1货币单位能兑换多少该站点的余额值（具体货币单位不重要）
  },
  "preference": {  // 这里面存储用户的偏好设置，即用户对该网站做的一些自定义配置等内容，根据应用具体实现设计字段细节
    "currency": "CNY", // 货币单位，默认显示¥符号，用户可切换其他货币单位显示
  },
  "models": [ // 这里面是主要的模型价格信息
    {
      "name": "gpt4", // 模型名
      "type": "tokens", // tokens/times 即按次收费还是按token收费
      "rateBaseline": 0.002,  // 每 1k Tokens 的价格基数，通常中转设置为 0.002
      "modelRate": 1, // 模型基础倍率
      "completeRate": 1, // 补全倍率
      "perPrice": 1,  // 按次收费时，每次调用的额度消耗基数，仅在按次计费有效
      "supportGroups": ["default", "premium", "official"]  // 模型支持的分组（只有指定分组才能调用该模型）
    }
   ...
  ],
  "groups": [
    {
      "id": "default", // 分组id，用于标识分组
      "name": "默认用户组",
      "rate": 1, // 分组使用倍率
    }
  ]
}
```

### 8.2 真实成本计算规则

基于标准化记录，计算真实成本：

- **1M 真实输入成本** = rateBaseline × modelRate × groupRate × 1000 ÷ rechargeRatio
- **1M 真实输出成本** = rateBaseline × modelRate × completeRate × groupRate × 1000 ÷ rechargeRatio

### 8.3 源站数据转换策略

采用「按来源可插拔解析器」架构，将不同源站的原始数据转换为上述标准化格式

### 8.4 解析失败策略

- 记录并可回放 `raw_payload`，提供可视化诊断
- 支持增量 parser 与回放调试、监控报警
- 新来源扩展：新增 `ParserAdapter`，只需实现字段映射与倍率计算规则
